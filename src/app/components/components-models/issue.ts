export interface Issue {
    id: string;
    type: string;
    issueTitle: string;
    issueDescription: string;
    issueDate: Date;
    issueStatus: string;
    issueClosingDate: Date;
    subcon: string;
}
